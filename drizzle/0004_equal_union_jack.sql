DROP TABLE `movies`;--> statement-breakpoint
/*
 SQLite does not support "Dropping foreign key" out of the box, we do not generate automatic migration for that, so it has to be done manually
 Please refer to: https://www.techonthenet.com/sqlite/tables/alter_table.php
                  https://www.sqlite.org/lang_altertable.html

 Due to that we don't generate migration automatically and it has to be done manually
*/--> statement-breakpoint
ALTER TABLE containers ADD `status` text;--> statement-breakpoint
ALTER TABLE items ADD `status` text;--> statement-breakpoint
ALTER TABLE projects ADD `createdByUserId` integer NOT NULL REFERENCES users(id);--> statement-breakpoint
ALTER TABLE subContainers ADD `status` text;--> statement-breakpoint
/*
 SQLite does not support "Creating foreign key on existing column" out of the box, we do not generate automatic migration for that, so it has to be done manually
 Please refer to: https://www.techonthenet.com/sqlite/tables/alter_table.php
                  https://www.sqlite.org/lang_altertable.html

 Due to that we don't generate migration automatically and it has to be done manually
*/--> statement-breakpoint
ALTER TABLE `projects` DROP COLUMN `userId`;--> statement-breakpoint
ALTER TABLE `roles` DROP COLUMN `description`;